import { createStore } from 'vuex';

const store = createStore({
  modules: {
    // cart: cartModule,
    // products: productsModule,
  },
  state() {},
  mutations() {},
  actions() {},
  getters() {}
});

export default store;
